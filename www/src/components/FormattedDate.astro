---
import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";

dayjs.extend(utc);

interface Props {
  value: string;
}

const { value } = Astro.props;
const formattedDate = dayjs.utc(value).format("MMMM D, YYYY");
---

<time datetime={value}>
  {formattedDate}
</time>
